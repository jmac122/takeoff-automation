# Docker Environment Configuration
# 
# IMPORTANT: Copy this file to docker/.env (NOT the project root!)
#   cp docker-env.example docker/.env
#
# Then edit docker/.env with your actual values.
# The .env file must be in the docker/ folder for Docker Compose to read it.

APP_ENV=development
SECRET_KEY=docker-secret-key-32-chars-for-development-only

# Database (Docker internal networking)
DATABASE_URL=postgresql+asyncpg://forgex:forgex@db:5432/forgex

# Redis/Celery (Docker internal networking)
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# Storage (MinIO - Docker internal networking)
STORAGE_ENDPOINT=minio:9000
STORAGE_BUCKET=takeoff-documents
# MinIO credentials (change from defaults for production!)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# LLM Provider Configuration (Optional - leave empty for Phase 1A)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_AI_API_KEY=
XAI_API_KEY=
DEFAULT_LLM_PROVIDER=anthropic
LLM_FALLBACK_PROVIDERS=

# Google Cloud Vision (OCR - Required for Phase 1B)
# Path to service account JSON key file (inside container)
# The credentials/ folder is mounted from project root
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/google-cloud-key.json

# Setup Instructions:
# 1. Create GCP service account: gcloud iam service-accounts create takeoff-ocr
# 2. Generate key: gcloud iam service-accounts keys create credentials/google-cloud-key.json --iam-account=takeoff-ocr@PROJECT_ID.iam.gserviceaccount.com
# 3. Enable Cloud Vision API: gcloud services enable vision.googleapis.com
# 4. The credentials/ folder is automatically mounted to /app/credentials in containers